import styled from 'styled-components';

const CountWrapper = styled.div`
    display:flex;
`;

const CountTitle = styled.span`
    color: #fff;
    font-size: 2rem;
    margin: 0;
`;

export {
    CountWrapper,
    CountTitle
}